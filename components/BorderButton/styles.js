import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    marginBottom: 20
  },
  button: {
    borderWidth: 1,
    borderRadius: 5,
    borderColor: 'black',
    textAlign: 'center',
    padding: 20,
    width: '100%',
    backgroundColor: '#fff',
    marginBottom: 10,
    color: '#E3446C',
    overflow: 'hidden',
    fontSize: 20
  }
})
