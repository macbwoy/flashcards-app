import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    borderWidth: 1,
    borderRadius: 5,
    borderColor: '#000',
    backgroundColor: '#fff',
    marginBottom: 20,
    paddingLeft: 20,
    overflow: 'hidden'
  },
  textInput: {
    color: '#858383',
    height: 40,
    fontSize: 20
  }
})
